# 任务卡 TaskCard-019
版本：v1.0 | 日期：2026-05-05 | 来源：PM

## 基本信息
- **任务编号**：TC-M6-01-niuniu
- **任务名称**：后端交付——性能优化 + 监控告警 + Docker 部署
- **任务类型**：部署 / 优化
- **截止时间**：2026-05-21 24:00
- **前置依赖**：TC-M5-01（实盘能力就绪）
- **所属终端**：niuniu（Go 业务后端）
- **所属里程碑**：M6（正式发布）
- **优先级**：🔴 高

---

## 任务内容

### 1. 性能优化
- 数据库查询优化（索引、批量操作）
- Redis 缓存命中率优化（盘口数据 TTL 调优）
- WebSocket 连接数上限（目标：支持 100 并发连接）
- 接口响应：p99 ≤ 500ms

### 2. 监控告警
- 日志：结构化日志（JSON 格式），按级别（INFO/WARN/ERROR）
- 监控指标：CPU、内存、数据库连接池、Redis 连接数、WebSocket 连接数
- 告警：WebSocket 向管理员推送系统异常（ERROR 日志触发）

### 3. Docker 构建
- backend/Dockerfile（多阶段构建，Go 二进制 + 最小化镜像）
- docker-compose.yml（含 Go/Gin :8080 + PostgreSQL + Redis）
- 相较 Phase 1 骨架版本：完整功能 + 监控 + 告警

### 4. 数据备份
- PostgreSQL 每日备份脚本（凌晨 2 点执行）
- 备份保留 7 天
- Redis 持久化（RDB + AOF）

### 5. 交付物整理
- 部署文档：README 包含 Docker 部署步骤
- 接口文档：Swagger（/swagger/）
- 测试用例覆盖：核心功能单元测试 + 集成测试

---

## 交付物
- [ ] p99 接口响应 ≤ 500ms
- [ ] 日志结构化（JSON），含 request_id
- [ ] 系统异常（ERROR）→ WebSocket alert 推送
- [ ] Docker 镜像构建成功（docker build）
- [ ] docker-compose up -d 启动无报错
- [ ] PostgreSQL 自动备份脚本存在且可执行
- [ ] Swagger 文档可访问（/swagger/）
- [ ] 单元测试覆盖率 ≥ 60%

---

## 审核点
1. 压测 100 并发 → p99 ≤ 500ms
2. 人为触发 ERROR 日志 → 管理员 WS 收到 alert
3. docker build 成功，镜像大小合理（<200MB）
4. docker-compose up -d → 全部服务启动成功
5. 备份脚本执行成功（测试运行）