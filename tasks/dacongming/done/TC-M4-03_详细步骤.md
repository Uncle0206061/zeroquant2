# TC-M4-03 详细执行步骤
> 目标：移动端适配（Vant，仅竖屏）
> 前置：TC-M1-02、TC-M4-02（骨架就绪 + PC持仓/订单页面）
> 说明：页面路由复用 PC 组件，数据相同，布局适配移动端

---

## Phase 0：确认前置环境

| 检查项 | 操作 | 预期 |
|--------|------|------|
| Vant | `grep vant package.json` | 有 vant ^4.x |
| Pinia 持久化 | `grep pinia-plugin-persistedstate` | 已装 |
| 移动端组件 | 有 `@vant` 相关组件 | 已装 |
| PC 组件 | `ls src/views/` | 有 Position.vue、Orders.vue、Backtest.vue、StrategyEditor.vue |
| Vite 配置 | `cat vite.config.ts` | 可配置 DPR / 移动端 viewport |

---

## Phase 1：Vant 基础配置

### 步骤 1.1：安装移动端额外依赖
```bash
cd D:\ZeroQuant2\frontend
pnpm add vant @vant/auto-import-resolver
pnpm add -D unplugin-vue-components unplugin-auto-import
```
或（npm）
```bash
npm install vant @vant/auto-import-resolver
npm install -D unplugin-vue-components unplugin-auto-import
```

### 步骤 1.2：更新 `vite.config.ts`（移动端 + PC 共用配置）
```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { VantResolver } from '@vant/auto-import-resolver'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [VantResolver()],
      imports: ['vue', 'vue-router', 'pinia']
    }),
    Components({
      resolvers: [VantResolver()]
    })
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': { target: 'http://localhost:8080', changeOrigin: true },
      '/data': { target: 'http://localhost:8080', changeOrigin: true }
    }
  },
  // 移动端 DPR 优化
  optimizeDeps: {
    include: ['vant', 'echarts']
  }
})
```

---

## Phase 2：移动端路由与布局

### 步骤 2.1：创建移动端主布局 `src/layouts/MobileLayout.vue`
```vue
<template>
  <div class="mobile-layout" :class="{ landscape: isLandscape }">
    <!-- 横屏提示 -->
    <div v-if="isLandscape" class="rotate-tip">
      <div class="rotate-icon">📱↻</div>
      <div>请使用竖屏浏览</div>
    </div>

    <!-- 竖屏主体 -->
    <template v-if="!isLandscape">
      <!-- 顶部导航栏 -->
      <van-nav-bar
        :title="pageTitle"
        left-arrow
        @click-left="$router.back()"
      >
        <template #right>
          <NotificationMobile /> <!-- 移动端简化通知 -->
        </template>
      </van-nav-bar>

      <!-- 内容区 -->
      <div class="mobile-content">
        <router-view />
      </div>

      <!-- 底部 Tab 栏 -->
      <van-tabbar v-model="activeTab" route>
        <van-tabbar-item to="/mobile/position" icon="cash">持仓</van-tabbar-item>
        <van-tabbar-item to="/mobile/orders" icon="orders">订单</van-tabbar-item>
        <van-tabbar-item to="/mobile/backtest" icon="chart">回测</van-tabbar-item>
        <van-tabbar-item to="/mobile/strategy" icon="setting">策略</van-tabbar-item>
      </van-tabbar>
    </template>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onUnmounted } from 'vue'
import { useRoute } from 'vue-router'
import NotificationMobile from '../components/NotificationMobile.vue'

const route = useRoute()
const activeTab = ref('/mobile/position')
const isLandscape = ref(false)

// 检测横屏
const checkOrientation = () => {
  isLandscape.value = window.innerWidth > window.innerHeight
}

const pageTitle = computed(() => {
  const map: Record<string, string> = {
    '/mobile/position': '我的持仓',
    '/mobile/orders': '订单列表',
    '/mobile/backtest': '回测分析',
    '/mobile/strategy': '策略管理',
    '/mobile/strategy/editor': '策略编辑'
  }
  return map[route.path] || 'ZeroQuant'
}

onMounted(() => {
  checkOrientation()
  window.addEventListener('resize', checkOrientation)
})

onUnmounted(() => {
  window.removeEventListener('resize', checkOrientation)
})
</script>

<style scoped>
.mobile-layout { height: 100vh; display: flex; flex-direction: column; }
.rotate-tip {
  flex: 1; display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  background: #f7f8fa; font-size: 16px; color: #646566;
}
.rotate-icon { font-size: 48px; margin-bottom: 16px; }
.mobile-content { flex: 1; overflow-y: auto; padding: 12px; }
</style>
```

---

## Phase 3：移动端持仓页面

### 步骤 3.1：创建 `src/views/mobile/Position.vue`
```vue
<template>
  <div class="mobile-position">
    <!-- 账户概览卡片 -->
    <van-card>
      <template #tags>
        <van-tag type="primary">总市值</van-tag>
      </template>
      <template #price>
        <span class="total-value">{{ formatMoney(totalMarketValue) }}</span>
      </template>
      <template #num>
        <span :class="totalProfitLoss >= 0 ? 'profit-up' : 'profit-down'">
          {{ totalProfitLoss >= 0 ? '+' : '' }}{{ formatMoney(totalProfitLoss) }}
        </span>
      </template>
    </van-card>

    <!-- 持仓列表 -->
    <van-pull-refresh v-model="refreshing" @refresh="onRefresh">
      <van-list>
        <div
          v-for="pos in positions"
          :key="pos.id"
          class="pos-card"
          @click="showDetail(pos)"
        >
          <div class="pos-left">
            <div class="pos-name">{{ pos.stockName }}</div>
            <div class="pos-code">{{ pos.stockCode }}</div>
          </div>
          <div class="pos-mid">
            <div class="pos-price">{{ pos.currentPrice.toFixed(2) }}</div>
            <div class="pos-cost">成本 {{ pos.avgCost.toFixed(2) }}</div>
          </div>
          <div class="pos-right">
            <div class="pos-qty">{{ pos.quantity }}股</div>
            <div :class="pos.profitRate >= 0 ? 'profit-up' : 'profit-down'">
              {{ pos.profitRate >= 0 ? '+' : '' }}{{ (pos.profitRate * 100).toFixed(2) }}%
            </div>
          </div>
        </div>
        <van-empty v-if="positions.length === 0" description="暂无持仓" />
      </van-list>
    </van-pull-refresh>

    <!-- 持仓详情弹窗 -->
    <van-popup v-model:show="detailVisible" position="bottom" round style="height:60%">
      <div class="detail-panel">
        <h3>{{ currentPos?.stockName }} ({{ currentPos?.stockCode }})</h3>
        <van-cell-group>
          <van-cell title="持仓数量" :value="currentPos?.quantity + ' 股'" />
          <van-cell title="成本价" :value="currentPos?.avgCost?.toFixed(2)" />
          <van-cell title="当前价" :value="currentPos?.currentPrice?.toFixed(2)" />
          <van-cell title="市值" :value="formatMoney(currentPos?.marketValue || 0)" />
          <van-cell title="盈亏" :value="formatMoney(currentPos?.profitLoss || 0)" />
        </van-cell-group>
      </div>
    </van-popup>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { PullRefresh, List, Card, Tag, Popup, CellGroup, Cell } from 'vant'
import { usePosition } from '../../composables/usePosition'

const refreshing = ref(false)
const detailVisible = ref(false)
const currentPos = ref<any>(null)

const { positions, loading, fetchPositions } = usePosition()

const totalMarketValue = computed(() =>
  positions.value.reduce((s, p) => s + p.marketValue, 0)
)
const totalProfitLoss = computed(() =>
  positions.value.reduce((s, p) => s + p.profitLoss, 0)
)

const formatMoney = (v: number) => v >= 0 ? `¥${v.toFixed(2)}` : `-¥${Math.abs(v).toFixed(2)}`

const onRefresh = async () => {
  await fetchPositions()
  refreshing.value = false
}

const showDetail = (pos: any) => {
  currentPos.value = pos
  detailVisible.value = true
}
</script>

<style scoped>
.mobile-position { padding: 12px; }
.total-value { font-size: 20px; font-weight: 700; }
.profit-up { color: #ee0a24; }
.profit-down { color: #07c160; }
.pos-card {
  background: #fff; border-radius: 8px; padding: 14px;
  margin-bottom: 10px; display: flex; justify-content: space-between;
  box-shadow: 0 1px 4px rgba(0,0,0,0.1);
}
.pos-left { text-align: left; }
.pos-name { font-size: 15px; font-weight: 600; }
.pos-code { font-size: 12px; color: #969799; }
.pos-mid { text-align: center; }
.pos-price { font-size: 15px; font-weight: 600; }
.pos-cost { font-size: 12px; color: #646566; }
.pos-right { text-align: right; }
.pos-qty { font-size: 13px; color: #323233; }
</style>
```

---

## Phase 4：移动端订单页面

### 步骤 4.1：创建 `src/views/mobile/Orders.vue`
```vue
<template>
  <div class="mobile-orders">
    <!-- 状态筛选标签 -->
    <van-tabs v-model:active="activeTab" swipeable class="status-tabs">
      <van-tab title="全部" name="" />
      <van-tab title="待成交" name="pending" />
      <van-tab title="已成交" name="filled" />
      <van-tab title="已撤单" name="cancelled" />
    </van-tabs>

    <!-- 订单列表 -->
    <van-pull-refresh v-model="refreshing" @refresh="onRefresh">
      <van-list>
        <div
          v-for="order in filteredOrders"
          :key="order.order_id"
          class="order-card"
        >
          <div class="order-header">
            <span class="order-stock">{{ order.stock_name }}</span>
            <van-tag :type="statusType[order.status]">{{ statusLabel[order.status] }}</van-tag>
          </div>
          <div class="order-body">
            <div>
              <span class="label">方向：</span>
              <span :class="order.direction === 1 ? 'buy-text' : 'sell-text'">
                {{ order.direction === 1 ? '买入' : '卖出' }}
              </span>
            </div>
            <div><span class="label">价格：</span>{{ order.price }}</div>
            <div><span class="label">数量：</span>{{ order.quantity }}</div>
          </div>
          <div class="order-footer">
            <span class="order-time">{{ formatTime(order.created_at) }}</span>
            <van-button
              v-if="order.status === 'pending'"
              size="small"
              type="danger"
              @click="handleCancel(order.order_id)"
            >
              撤单
            </van-button>
          </div>
        </div>
        <van-empty v-if="filteredOrders.length === 0" description="暂无订单" />
      </van-list>
    </van-pull-refresh>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { PullRefresh, List, Tabs, Tab, Tag, Button, showToast } from 'vant'
import { useOrders } from '../../composables/useOrders'

const refreshing = ref(false)
const activeTab = ref('')

const { filteredOrders, fetchOrders, cancelOrder } = useOrders()

const statusLabel: Record<string, string> = {
  pending: '待成交', filled: '已成交', cancelled: '已撤单', rejected: '已拒绝'
}
const statusType: Record<string, string> = {
  pending: 'warning', filled: 'success', cancelled: 'default', rejected: 'danger'
}

const formatTime = (d: string) => d ? d.substring(0, 16).replace('T', ' ') : '-'

const onRefresh = async () => {
  await fetchOrders()
  refreshing.value = false
}

const handleCancel = async (orderId: string) => {
  try {
    await cancelOrder(orderId)
    showToast('撤单成功')
  } catch (e: any) {
    showToast(e.message || '撤单失败')
  }
}
</script>

<style scoped>
.mobile-orders { padding: 12px; }
.status-tabs { margin-bottom: 12px; }
.order-card {
  background: #fff; border-radius: 8px; padding: 14px;
  margin-bottom: 10px; box-shadow: 0 1px 4px rgba(0,0,0,0.1);
}
.order-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
.order-stock { font-size: 15px; font-weight: 600; }
.order-body { display: flex; gap: 16px; flex-wrap: wrap; font-size: 13px; }
.order-body .label { color: #969799; }
.buy-text { color: #ee0a24; }
.sell-text { color: #07c160; }
.order-footer { display: flex; justify-content: space-between; align-items: center; margin-top: 8px; }
.order-time { font-size: 12px; color: #969799; }
</style>
```

---

## Phase 5：移动端回测结果页面

### 步骤 5.1：创建 `src/views/mobile/Backtest.vue`
```vue
<template>
  <div class="mobile-backtest">
    <!-- 回测结果卡片 -->
    <van-card v-if="result" class="result-card">
      <template #title>
        <div class="result-title">回测结果</div>
      </template>
      <template #tags>
        <van-tag type="success">年化 {{ result.annual_return || '—' }}</van-tag>
        <van-tag type="warning">最大回撤 {{ result.max_drawdown || '—' }}</van-tag>
        <van-tag type="primary">夏普 {{ result.sharpe_ratio || '—' }}</van-tag>
      </template>
    </van-card>

    <!-- 资金曲线图（移动端简化） -->
    <van-card title="资金曲线" class="chart-card">
      <div ref="equityChartRef" style="height:200px;width:100%" />
    </van-card>

    <!-- 成交记录表 -->
    <van-card title="成交记录" class="trade-list">
      <van-table :data="result?.trades || []">
        <van-table-column prop="time" label="时间" width="120" />
        <van-table-column prop="stockCode" label="代码" width="90" />
        <van-table-column prop="direction" label="方向" width="50">
          <template #default="{ row }">
            {{ row.direction === 'buy' ? '买' : '卖' }}
          </template>
        </van-table-column>
        <van-table-column prop="price" label="价格" width="70" />
        <van-table-column prop="quantity" label="数量" width="50" />
      </van-table>
    </van-card>

    <van-empty v-if="!result" description="暂无回测结果，请先发起回测" />
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { Card, Tag, Table, TableColumn, Empty } from 'vant'
import * as echarts from 'echarts'

const result = ref<any>(null) // 移动端直接接收 PC 端缓存的数据（Pinia 持久化）
const equityChartRef = ref<HTMLElement>()

// 从 Pinia store 中恢复 PC 端已缓存的回测结果
import { useBacktestStore } from '../stores/backtest'
const backtestStore = useBacktestStore()
result.value = backtestStore.lastResult
</script>

<style scoped>
.mobile-backtest { padding: 12px; }
.result-card { margin-bottom: 12px; }
.chart-card { margin-bottom: 12px; }
.trade-list { margin-bottom: 12px; }
</style>
```

---

## Phase 6：Pinia 持久化（离线缓存）

### 步骤 6.1：创建 `src/stores/backtest.ts`
```typescript
import { defineStore } from 'pinia'

export const useBacktestStore = defineStore('backtest', {
  state: () => ({
    lastResult: null as any,
    lastForm: null as any,
  }),
  actions: {
    setResult(result: any) {
      this.lastResult = result
    }
  },
  persist: true // 需要 pinia-plugin-persistedstate
})
```

### 步骤 6.2：安装并配置持久化插件
```bash
pnpm add pinia-plugin-persistedstate
```
更新 `src/main.ts`：
```typescript
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'
app.use(createPinia().use(piniaPluginPersistedstate))
```

---

## Phase 7：横屏检测 + 离线只看不操作

### 步骤 7.1：创建移动端离线检测 `src/composables/useNetworkStatus.ts`
```typescript
import { ref, onMounted, onUnmounted } from 'vue'

export function useNetworkStatus() {
  const isOnline = ref(navigator.onLine)

  const update = () => { isOnline.value = navigator.onLine }

  onMounted(() => {
    window.addEventListener('online', update)
    window.addEventListener('offline', update)
  })

  onUnmounted(() => {
    window.removeEventListener('online', update)
    window.removeEventListener('offline', update)
  })

  return { isOnline }
}
```

### 步骤 7.2：在移动端组件中应用
在 Position.vue / Orders.vue / Backtest.vue 中：
```vue
<script setup lang="ts">
import { useNetworkStatus } from '../../composables/useNetworkStatus'
const { isOnline } = useNetworkStatus()
</script>
<template>
  <div v-if="!isOnline" class="offline-tip">
    <van-notice-bar text="当前离线，仅展示缓存数据，下单/撤单等功能暂时不可用" />
  </div>
  <!-- 禁止下单按钮在离线时禁用 -->
  <van-button v-if="isOnline" type="danger" @click="submitOrder">下单</van-button>
  <van-button v-else type="default" disabled>离线不可下单</van-button>
</template>
```

---

## Phase 8：移动端路由注册

### 步骤 8.1：更新 `src/router/index.ts`
添加移动端路由（独立路由，与 PC 分开）：
```typescript
{
  path: '/mobile',
  component: () => import('../layouts/MobileLayout.vue'),
  children: [
    { path: 'position', component: () => import('../views/mobile/Position.vue') },
    { path: 'orders', component: () => import('../views/mobile/Orders.vue') },
    { path: 'backtest', component: () => import('../views/mobile/Backtest.vue') },
    { path: 'strategy', component: () => import('../views/mobile/Strategy.vue') },
    { path: 'strategy/editor', component: () => import('../views/mobile/StrategyEditor.vue') },
  ]
}
```

---

## Phase 9：完成自检

| 检查项 | 操作 | 预期 |
|--------|------|------|
| 移动端启动 | 手机浏览器访问 localhost:5173/mobile/position | 显示移动端布局（底部Tab栏） |
| 横屏提示 | 手机横屏时访问 | 显示「请使用竖屏」提示 |
| 持仓数据 | 移动端 /mobile/position | 与 PC 端数据完全一致 |
| 订单列表 | 移动端 /mobile/orders | 筛选和撤单功能正常 |
| 回测图表 | 移动端 /mobile/backtest | ECharts 图表正常显示 |
| 离线缓存 | 断网后访问 | 显示最后缓存数据，无下单按钮 |
| 离线缓存 | PC 回测后移动端查看 | 展示 PC 端缓存的回测结果 |

---

## 交付物清单

- [ ] `vite.config.ts` 更新（含 Vant 按需导入 + 自动导入）
- [ ] `src/layouts/MobileLayout.vue`（横屏检测 + 底部Tab栏）
- [ ] `src/views/mobile/Position.vue`（移动端持仓页面，Vant Card + List）
- [ ] `src/views/mobile/Orders.vue`（移动端订单页面，筛选 + 撤单）
- [ ] `src/views/mobile/Backtest.vue`（移动端回测结果，ECharts 图表）
- [ ] `src/composables/useNetworkStatus.ts`（网络状态检测）
- [ ] `src/stores/backtest.ts`（回测结果持久化）
- [ ] PC 与移动端功能一致（数据相同）
- [ ] 横屏时显示切换竖屏提示
- [ ] 断网时显示缓存数据，无下单功能