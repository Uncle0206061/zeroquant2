# TC-M1-03 执行计划 · Python数据服务骨架初始化

> **任务编号**：TC-M1-03-dataofficer  
> **接管者**：零号（2026-05-13 接手，原数据官未开始）  
> **截止日期**：2026-05-08（已过期5天）  
> **执行顺序**：第1个任务（一切数据服务功能的基础）

---

## 📋 交付物清单

| # | 交付物 | 路径 | 验收标准 |
|---|--------|------|----------|
| 1 | 项目目录结构 | `src/data/` | 所有子目录存在，层次清晰 |
| 2 | requirements.txt | `src/data/requirements.txt` | 包含全部8个依赖包 |
| 3 | 虚拟环境可运行 | Python 3.10+ venv | `uvicorn app.main:app` 启动无报错 |
| 4 | PostgreSQL 连通 | `src/data/app/config.py` | `data_` 表前缀连接测试通过 |
| 5 | Redis 连通 | `src/data/app/config.py` | SET/GET 测试通过 |
| 6 | 健康检查接口 | `src/data/app/api/v1/health.py` | `GET /data/v1/health` → `{code:0, data:{status:"ok"}}` |
| 7 | API 路由预置文件 | `src/data/app/api/v1/` | 6个路由文件存在，均含待办注释 |
| 8 | 接口草案文档 | `docs/data-api-draft-v0.1.md` | 含全部路由表+响应格式 |

---

## 🔨 分步执行清单

### Step 0 · 环境确认
- [ ] 确认 Python 3.10+ 可用：`python --version`
- [ ] 确认目标目录：`D:\ZeroQuant2\src\data\` 存在（不存在则创建）
- [ ] 确认 NAS 对齐：`Z:\ZeroQuant2\src\data\` 已清空/同步

### Step 1 · requirements.txt
```txt
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
redis==5.0.8
pandas==2.2.3
apscheduler==3.10.4
pydantic==2.9.0
alembic==1.13.2
python-dotenv==1.0.1
akshare==1.14.18
```
- [ ] 写入 `src/data/requirements.txt`

### Step 2 · 创建目录结构
```
src/data/
├── app/
│   ├── __init__.py
│   ├── main.py              ← FastAPI 入口，挂载路由
│   ├── config.py            ← 配置（DB/Redis连接）
│   ├── api/
│   │   ├── __init__.py
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── health.py    ← [实现] GET /data/v1/health
│   │       ├── market.py    ← [预置] 实时行情（TODO注释）
│   │       ├── kline.py     ← [预置] K线接口（TODO注释）
│   │       ├── orderbook.py ← [预置] Level2盘口（TODO注释）
│   │       ├── sector.py    ← [预置] 板块接口（TODO注释）
│   │       └── filter.py    ← [预置] 筛选接口（TODO注释）
│   ├── collectors/
│   │   ├── __init__.py
│   │   ├── akshare_collector.py  ← [预置框架]
│   │   ├── tushare_collector.py ← [预置框架]
│   │   └── level2_collector.py ← [预置框架]
│   ├── models/
│   │   ├── __init__.py
│   │   └── base.py          ← SQLAlchemy Base
│   ├── schemas/
│   │   ├── __init__.py
│   │   └── base.py          ← Pydantic BaseModel
│   ├── services/
│   │   ├── __init__.py
│   │   └── placeholder.py
│   └── schedulers/
│       ├── __init__.py
│       └── placeholder.py
├── alembic/
│   ├── env.py
│   └── placeholder
├── tests/
│   ├── __init__.py
│   └── test_health.py       ← 健康检查单元测试
├── .env.example             ← 环境变量模板
└── Dockerfile
```
- [ ] 创建全部目录和 `__init__.py`
- [ ] 预置文件写入 TODO 注释占位

### Step 3 · config.py（连接配置）
- [ ] 从环境变量读取：`DATABASE_URL`、`REDIS_URL`
- [ ] PostgreSQL：`postgresql://user:pass@localhost:5432/data`，表前缀 `data_`
- [ ] Redis：`redis://localhost:6379/1`，key 前缀 `data:`
- [ ] 写入 `src/data/app/config.py`

### Step 4 · main.py（FastAPI 入口）
```python
from fastapi import FastAPI
from app.api.v1 import health, market, kline, orderbook, sector, filter

app = FastAPI(title="ZeroQuant Data Service", version="0.1.0")

app.include_router(health.router, prefix="/data/v1", tags=["health"])
# 其他路由暂时注册，但返回 "pending implementation"
```
- [ ] 写入 `src/data/app/main.py`

### Step 5 · health.py（健康检查实现）
```python
from fastapi import APIRouter
router = APIRouter()

@router.get("/health")
def health_check():
    return {"code": 0, "data": {"status": "ok"}}
```
- [ ] 写入 `src/data/app/api/v1/health.py`

### Step 6 · 虚拟环境安装
```bash
cd D:\ZeroQuant2\src\data
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```
- [ ] 执行安装，验证无报错

### Step 7 · 连接测试
```bash
# PostgreSQL 测试
python -c "from app.config import engine; print(engine.execute('SELECT 1'))"

# Redis 测试
python -c "from app.config import redis_client; print(redis_client.set('test','ok')); print(redis_client.get('test'))"
```
- [ ] PostgreSQL 连通测试通过
- [ ] Redis 连通测试通过

### Step 8 · 服务启动验证
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8081 --reload
```
- [ ] 服务启动成功（无报错）
- [ ] `curl http://localhost:8081/data/v1/health` 返回 `{code:0, data:{status:"ok"}}`

### Step 9 · akshare 数据采集测试
```python
import akshare as ak
df = ak.stock_zh_a_spot_em()
print(df.head())
```
- [ ] akshare 可正常拉取数据

### Step 10 · docs/data-api-draft-v0.1.md
- [ ] 写入 `docs/data-api-draft-v0.1.md`
  - 包含全部 `/data/v1/` 路由表
  - 包含各接口响应格式
  - 包含数据源说明（akshare/Tushare Pro/Level-2）

### Step 11 · NAS 备份同步
```bash
robocopy D:\ZeroQuant2\src\data Z:\ZeroQuant2\src\data /MIR /R:3
```
- [ ] NAS `Z:\ZeroQuant2\src\data\` 与本地同步

### Step 12 · 写入完成卡
- [ ] 写入 `D:\ZeroQuant2\tasks\dataofficer\done\TC-M1-03.md`

---

## ✅ 完成标准

1. `uvicorn app.main:app --port 8081` 启动无报错
2. `GET /data/v1/health` → `{code:0, data:{status:"ok"}}`
3. `GET /docs` → FastAPI Swagger UI 可访问
4. akshare 数据源可拉取（测试一次采集）
5. `docs/data-api-draft-v0.1.md` 包含全部路由清单

---

## ⚠️ 阻塞处理
如遇阻塞，在 `tasks\dataofficer\inbox\` 下写 `TC-M1-03_blocked.md`，注明问题，等你回复后再继续。