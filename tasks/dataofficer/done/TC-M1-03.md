# 任务卡 TaskCard-008
版本：v1.0 | 日期：2026-05-05 | 来源：PM

## 基本信息
- **任务编号**：TC-M1-03-dataofficer
- **任务名称**：Python 数据服务项目骨架初始化
- **任务类型**：配置搭建 / 代码编写
- **截止时间**：2026-05-08 24:00
- **前置依赖**：TC-T0-03（数据服务环境就绪）
- **所属终端**：dataofficer（Python 数据服务）
- **所属里程碑**：M1（项目骨架就绪）
- **优先级**：🔴 高

---

## 任务内容

### 1. 项目结构初始化
按「数据官个人开发计划 V1.2」目录规范初始化 Python FastAPI 项目：

```
data/
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── health.py    ← Day1 实现
│   │       ├── market.py    ← 行情接口（预置）
│   │       ├── kline.py     ← K线接口（预置）
│   │       ├── orderbook.py ← Level2 盘口接口（预置）
│   │       ├── sector.py    ← 板块接口（预置）
│   │       └── filter.py    ← 筛选接口（预置）
│   ├── collectors/
│   │   ├── akshare_collector.py
│   │   ├── tushare_collector.py
│   │   └── level2_collector.py
│   ├── models/             ← SQLAlchemy 模型
│   ├── schemas/            ← Pydantic 模型
│   ├── services/           ← 业务逻辑
│   ├── schedulers/         ← 定时任务
│   ├── config.py
│   └── main.py
├── alembic/               ← 数据库迁移
├── requirements.txt
└── Dockerfile
```

### 2. 环境 & 依赖
- Python 3.10+ 虚拟环境
- FastAPI + Uvicorn + SQLAlchemy + Redis + Pandas + APScheduler
- requirements.txt 完整

### 3. 数据库 & Redis 连接
- 连接 PostgreSQL（data 库，data_ 表前缀）
- 连接 Redis（data: 前缀缓存）

### 4. 实现健康检查接口
```
GET /data/v1/health
响应：{ "code": 0, "data": { "status": "ok" } }
```

### 5. 数据源接入（预置框架）
- akshare（Tushare Pro 备选）
- Level-2 五档盘口接入框架

### 6. WebSocket 骨架（预置配置）
预置心跳间隔10秒 / 断开自动重连最多10次间隔2秒 / 推送延迟≤200ms

### 7. 输出 Python 数据服务接口草案 v0.1
包含：/data/v1/ 下全部路由、响应格式、数据源说明

---

## 交付物
- [ ] data/ 目录结构完整，requirements.txt 完整
- [ ] Python 虚拟环境可运行（uvicorn main:app 启动无报错）
- [ ] PostgreSQL data 库连通（data_ 表前缀测试）
- [ ] Redis data: 前缀连通（SET/GET 测试）
- [ ] GET /data/v1/health 返回 {code:0, data:{status:"ok"}}
- [ ] data/app/api/v1/ 下所有接口路由预置文件已创建（返回待办注释即可）
- [ ] docs/data-api-draft-v0.1.md（含路由表 + 响应格式）

---

## 审核点
1. `uvicorn app.main:app --reload` 服务启动正常（端口 8081）
2. `curl http://localhost:8081/data/v1/health` 返回 code:0
3. akshare 数据源可拉取（运行一次采集测试）
4. docs/data-api-draft-v0.1.md 包含全部 /data/v1/ 路由清单

---

## 反馈要求
- 完成时：在 tasks/dataofficer/done/TC-M1-03.md 写入反馈卡
- 异常时：tasks/dataofficer/inbox/TC-M1-03_blocked.md 描述问题，等 PM 回复